//Converter Number para String usando variavel 'n' como exemplo.
/*
    String(n)
    n.toString()
*/
//Formantando Strings
/* 
    var s = 'Javascript'

    window.alert('Modo antigo de concatenar em ' + s)
    window.alert(`Modo novo de concatenar em ${s}`)

    s.length // Quantos caracteres a string tem
    s.toUpperCase() // Tudo para 'MAIUSCULA'
    s.toLowerCase() // Tudo para 'minuscula'
*/
//Converter numeros

/*
    var n1 = 1545.5

    n1.toFixed(2) == '1545.50' // Converte para duas casas decimais
    n1.toFixed(2).replace('.', ',') == 1545,50 // Converte de . para ,
    n1.toLocaleString('pt-BR', {style: 'currency', currency: 'BRL'}) // converte para moeda real (USD para USA - EUR para Europa)
*/
//Ordem precendência geral
/*
    Operadores aritimeticos == 5 + 2 // Primeiro
    operadores racionais a > b // segundo
    operadores logicos 5 + 2 >= a && 5 + 2 <= b // terceiro

    () ** / .. // primeiro
    > < >= .. // segundo
    ! & || // terceiro
*/
