function soma(n1=0, n2=0){ // se n1 ou n2 não for passado parametro, então ele considera 0
    return n1 + n2
}
console.log(soma(2, 5))
//console.log(soma(2)) // Caso não passe 1 parametro posso colocar lá em cima que ele considere outro numero.